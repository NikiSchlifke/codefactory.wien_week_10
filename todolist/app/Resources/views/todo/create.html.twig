{% extends 'base.html.twig' %}
{% block body %}
        <h2 class="page-header">Add Todo</h2>
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}

{% endblock %}

{% block javascripts %}

    <script>
        $('.tokenfield').tokenfield({
            autocomplete: {
                source: {{ all_categories | json_encode | raw}}, // raw(json_encode(categories)))
                delay: 100
            },
            showAutocompleteOnFocus: true
        });
        console.log('*');
    </script>
{% endblock %}